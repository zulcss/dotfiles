#
# Container aliases
# 
elinks() {
    docker run --rm -it \
    --name elinks elinks "$@"
}

aws() {
    docker run -it --rm \
        -v "${HOME}/.aws:/root/.aws" \
        aws "$@"
}

powershell() {
    docker run -it --rm powershell "$@"
}

nmap() {
    docker run -it --rm nmap "$@"
}
